const Koa = require('koa')
const app = new Koa()
const { connect } = require('../database/init')

app.use(async (ctx) => {
  ctx.body = '<h1>koa</h1>'
})

;(async () => {
  await connect()
})()

app.listen(3000, () => {
  console.log('something on 3000 port')
})
